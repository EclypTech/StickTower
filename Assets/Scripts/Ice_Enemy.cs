using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ice_Enemy : MonoBehaviour
{
    void Start()
    {

    }

    void FixedUpdate()
    {
        if (gameObject.transform.position.y >= 20 && gameObject.transform.position.y < 22)
        { 
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 40 && gameObject.transform.position.y < 42)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 60 && gameObject.transform.position.y < 62)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 80 && gameObject.transform.position.y < 82)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 100 && gameObject.transform.position.y < 102)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 120 && gameObject.transform.position.y < 122)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 140 && gameObject.transform.position.y < 142)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 160 && gameObject.transform.position.y < 162)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 180 && gameObject.transform.position.y < 182)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 200 && gameObject.transform.position.y < 202)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 220 && gameObject.transform.position.y < 222)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 240 && gameObject.transform.position.y < 242)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 260 && gameObject.transform.position.y < 262)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 280 && gameObject.transform.position.y < 282)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 300 && gameObject.transform.position.y < 302)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 320 && gameObject.transform.position.y < 322)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 340 && gameObject.transform.position.y < 342)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 360 && gameObject.transform.position.y < 362)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 380 && gameObject.transform.position.y < 382)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 400 && gameObject.transform.position.y < 402)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 420 && gameObject.transform.position.y < 422)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 440 && gameObject.transform.position.y < 442)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 460 && gameObject.transform.position.y < 462)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 480 && gameObject.transform.position.y < 482)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 500 && gameObject.transform.position.y < 502)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 520 && gameObject.transform.position.y < 522)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 540 && gameObject.transform.position.y < 542)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 560 && gameObject.transform.position.y < 562)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 580 && gameObject.transform.position.y < 582)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 600 && gameObject.transform.position.y < 602)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 620 && gameObject.transform.position.y < 622)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 640 && gameObject.transform.position.y < 642)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 680 && gameObject.transform.position.y < 682)
        {
            gameObject.SetActive(true); ;
        }
        else if (gameObject.transform.position.y >= 700 && gameObject.transform.position.y < 702)
        {
            gameObject.SetActive(true); ;
        }
        else
        {
            gameObject.SetActive(false);
        }
    }


}
